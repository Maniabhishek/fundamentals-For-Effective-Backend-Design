- A codec is a mechanism that encodes data into a compact, standardized format and decodes it back for transmission or storage.

1️⃣ Why codecs exist
- Raw data is usually:
  - Too large (audio/video)
  - Not transmittable efficiently
  - Not compatible across systems
- Codecs solve this by:
  - Compressing data
  - Standardizing formats
  - Enabling efficient storage & streaming


2️⃣ Types of codecs
- Media codecs (most common)
- Data / Serialization codecs
- Compression codecs (example Gzip, snappy)


#### Media Codecs
- 🎵 Audio codecs
  - MP3:	Lossy	Music streaming
  - AAC:	Lossy	YouTube, iOS
  - Opus:	Lossy	VoIP, WebRTC
  - FLAC:	Lossless	High-quality audio
- 🎥 Video codecs
  - H.264 (AVC):	Lossy	YouTube, Netflix
  - H.265 (HEVC):	Better compression	4K video
  - VP9:	Open-source	YouTube
  - AV1:	Best compression	Modern streaming

#### Data / Serialization codecs
- JSON:	REST APIs
- Protobuf:	gRPC


#### codecs used 
- Kafka → Avro vs Protobuf trade-offs
- gRPC → Protobuf codec
- APIs → JSON vs binary codecs
- Compression → Snappy vs Gzip
👉 Typical question:

### Why is Protobuf faster than JSON?
- Binary format
- Smaller payload
- No runtime parsing of text

### If designing a video platform:
- Encode once → multiple bitrates
- Serve via CDN
- Support HLS + DASH
- Use AV1 for VOD, H.264 for live


#### HLS / DASH are NOT compared with AV1 / H.264
- They solve different layers of the video stack.
- Codec → How video is compressed
- HLS / DASH → How video is delivered adaptively
- Netflix → DASH + AV1
- YouTube → DASH + AV1 / VP9 / H.264
- Apple TV → HLS + H.264
